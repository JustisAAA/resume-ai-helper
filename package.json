{
  "name": "resume-ai-helper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend3 && npm install && npx prisma generate && npm run build && cd ../frontend && npm install --include=dev && VITE_API_URL=/ npm run build",
    "start": "cd backend3 && npx prisma db push --accept-data-loss && NODE_ENV=production node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
